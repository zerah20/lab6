name: first_aid_quick_guide
description: A simple offline first aid quick guide using SQLite with seeded data and image support.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.18.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # Updated Supabase to latest stable — fixes the Android build issue
  supabase_flutter: ^2.10.3

  # Updated image_picker to latest stable version
  image_picker: ^1.2.1

  # Updated intl to latest compatible version
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - lib/assets/icon/app_icon.jpg
